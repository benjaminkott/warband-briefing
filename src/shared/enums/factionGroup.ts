/**
 * Where a faction belongs: `season` for one that the season adds, `expansion`
 * for one that came with the expansion and stays over all seasons. The
 * settings list the two apart.
 */
export enum FactionGroup {
  Season = 'season',
  Expansion = 'expansion'
}
