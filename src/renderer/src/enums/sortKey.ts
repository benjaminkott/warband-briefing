/** What the roster sorts by: the plan's order, or the column of the table whose head was clicked. */
export enum SortKey {
  Todo = 'todo',
  Plan = 'plan',
  Name = 'name',
  Ilvl = 'ilvl',
  Score = 'score',
  Key = 'key',
  Vault = 'vault',
  Gold = 'gold',
  Played = 'played',
  Updated = 'updated',
  Gear = 'gear'
}
