/** The pages of the settings, in the order of the nav. */
export enum SettingsSection {
  Setup = 'setup',
  Roster = 'roster',
  Tasks = 'tasks',
  Appearance = 'appearance',
  Keyboard = 'keyboard',
  App = 'app'
}
