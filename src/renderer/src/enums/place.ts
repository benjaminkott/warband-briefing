/**
 * Where a thing about a character stands: the card's head, a table cell,
 * the best matrix. The name and the links both take it; each sets its
 * type and spacing for the place.
 */
export enum Place {
  Card = 'card',
  Table = 'table',
  Matrix = 'matrix'
}
