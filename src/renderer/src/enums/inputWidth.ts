/**
 * How much of a form row the field takes: the long one that grows, the
 * short one for an id, the tiny one for a number - or the row's default.
 */
export enum InputWidth {
  Grow = 'grow',
  Small = 'small',
  Tiny = 'tiny'
}
