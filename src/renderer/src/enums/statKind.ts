/**
 * The two shapes a figure with its caption takes: the tile - one box, the
 * same on the summary bar, the gold view and the board - and the bare cell
 * of the character page's hero band.
 */
export enum StatKind {
  Tile = 'tile',
  Cell = 'cell'
}
