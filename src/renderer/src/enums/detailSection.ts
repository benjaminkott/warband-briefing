/**
 * The sections of the character page, in the order of its sub-navigation:
 * what resets first, as the page opens on it.
 */
export enum DetailSection {
  Week = 'week',
  Season = 'season',
  Gear = 'gear',
  Professions = 'professions',
  Inventory = 'inventory'
}
