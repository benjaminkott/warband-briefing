/** How the step reads on the tile: a reward to claim, a chore open, the week done, or nothing to say. */
export enum StepTone {
  Claim = 'claim',
  Open = 'open',
  Done = 'done',
  Quiet = 'quiet'
}
